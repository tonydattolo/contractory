import { Button } from "react-bootstrap"

export default function Wallets() {
  
  return (
    <div>
      <h1>Wallets</h1>
      <Button variant="primary">add current wallet to account</Button>
    </div>
  )
}